import React from 'react';
import DatePicker from 'shared/components/Datebox';

const DateSamples = () => (
  <div>
    <div className="dx-fieldset">
      <DatePicker fieldTitle="Field Title" stylingMode="underlined" isRtl={false} />
    </div>
  </div>
);

export default DateSamples;
